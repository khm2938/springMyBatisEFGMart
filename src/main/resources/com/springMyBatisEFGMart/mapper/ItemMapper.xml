<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springMyBatisEFGMart.mapper.ItemMapper">

  <select id="selectItemList" resultType="com.springMyBatisEFGMart.domain.Item">
    SELECT
      itemid        AS itemId,
      modelname     AS modelName,
      brand,
      category,
      color,
      shoesize      AS shoeSize,
      price,
      discountrate  AS discountRate,
      stockcount    AS stockCount
    FROM item
    ORDER BY itemid
  </select>

</mapper>
